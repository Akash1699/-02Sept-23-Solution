The Document Type Declaration  <!DOCTYPE> declaration in HTML is an instruction to the web browser about the version of HTML in which the webpage is written. It's placed at the very beginning of an HTML document, before the <html> tag.